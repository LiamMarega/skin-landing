---
import { Icon } from "astro-icon/components";

const testimonials = [
    {
        name: "Matias",
        role: "Graphic Designer",
        content:
            "El flujo de trabajo es de otro planeta. Mis diseños pasaron de verse amateur a nivel industrial en una semana.",
        initial: "M",
        color: "bg-blue-600",
    },
    {
        name: "Lucas",
        role: "Freelancer",
        content:
            "Lo que aprendí de IA aplicada al diseño de eventos me ahorra 3 horas por flyer. Una locura.",
        initial: "L",
        color: "bg-purple-600",
    },
    {
        name: "Julian",
        role: "Content Creator",
        content:
            "La mejor inversión que hice este año. El nivel de detalle en el temario es impecable.",
        initial: "J",
        color: "bg-emerald-600",
    },
];
---

<section
    class="relative w-full py-24 bg-black font-sans overflow-hidden"
    style="mask-image: linear-gradient(to bottom, transparent 0%, black 10%, black 90%, transparent 100%); -webkit-mask-image: linear-gradient(to bottom, transparent 0%, black 10%, black 90%, transparent 100%);"
>
    <!-- Decorative Glow -->
    <div
        class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] bg-blue-600/10 rounded-full blur-[120px] pointer-events-none"
    >
    </div>

    <div class="max-w-screen mx-auto px-6 relative z-10">
        <h2
            class="text-3xl md:text-5xl font-black text-center mb-16 text-white uppercase italic tracking-tighter"
        >
            Lo que dicen <span class="text-blue-500">Ellos</span>
        </h2>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            {
                testimonials.map((t) => (
                    <div class="group bg-zinc-900/40 backdrop-blur-sm border border-white/5 p-8 rounded-3xl hover:border-blue-500/30 transition-all duration-500 relative overflow-hidden">
                        <div class="absolute top-0 right-0 w-24 h-24 bg-blue-500/5 blur-3xl rounded-full" />

                        <div class="flex items-center gap-4 mb-6">
                            <div
                                class={`w-12 h-12 ${t.color} rounded-full flex items-center justify-center font-bold text-xl text-white`}
                            >
                                {t.initial}
                            </div>
                            <div>
                                <h4 class="text-white font-bold leading-none">
                                    {t.name}
                                </h4>
                                <p class="text-gray-500 text-sm mt-1">
                                    {t.role}
                                </p>
                            </div>
                        </div>

                        <div class="flex gap-1 mb-4 text-accent">
                            {[...Array(5)].map(() => (
                                <Icon
                                    name="lucide:star"
                                    class="w-4 h-4 fill-current"
                                />
                            ))}
                        </div>

                        <p class="text-gray-400 font-medium leading-relaxed italic">
                            "{t.content}"
                        </p>
                    </div>
                ))
            }
        </div>

        <div class="mt-20 flex justify-center items-center gap-12 opacity-30">
            <div class="text-white font-black italic tracking-tighter">
                +50 alumnos ya transformaron su carrera
            </div>
        </div>
    </div>
</section>
