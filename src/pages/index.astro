---
import "../styles/global.css";
import Hero from "@/components/Hero.astro";
import WhatWillYouAchieve from "@/components/WhatWillYouAchieve.astro";
import PricingCards from "@/components/PricingCards.astro";
import HowToRegister from "@/components/HowToRegister.astro";
import ComparisonTable from "@/components/ComparisonTable.astro";
import Syllabus from "@/components/Syllabus.astro";
import Testimonials from "@/components/Testimonials.astro";
import FAQ from "@/components/FAQ.astro";
import GradualBlur from "@/components/ui/backgrounds/GradualBlur/GradualBlur";
import BlobCursor from "@/components/ui/Cursor/BlobCursor";
import AnnouncementBar from "@/components/ui/AnnouncementBar.astro";
import { FloatingWhatsApp } from "@digicroz/react-floating-whatsapp";
---

<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<title>Diseñá Como Yo - Mentoría de Diseño Gráfico PRO</title>
		<meta
			name="description"
			content="En 30 días aprendé a diseñar flyers pro para eventos con Photoshop, Illustrator + IA y recuperá tu inversión con nuestro sistema de ventas."
		/>
	</head>
	<body class="bg-black text-white antialiased">
		<main>
			<div class="reveal"><Hero /></div>
			<div class="reveal"><WhatWillYouAchieve loading="lazy" /></div>
			<div class="reveal"><PricingCards loading="lazy" /></div>
			<div class="reveal"><HowToRegister loading="lazy" /></div>
			<div class="reveal"><ComparisonTable loading="lazy" /></div>
			<div class="reveal"><Syllabus loading="lazy" /></div>
			<div class="reveal"><Testimonials loading="lazy" /></div>
			<div class="reveal"><FAQ loading="lazy" /></div>
		</main>

		<script>
			const observerOptions = {
				root: null,
				rootMargin: "0px",
				threshold: 0.1,
			};

			const observer = new IntersectionObserver((entries) => {
				entries.forEach((entry) => {
					if (entry.isIntersecting) {
						entry.target.classList.add("reveal-visible");
					}
				});
			}, observerOptions);

			document.querySelectorAll(".reveal").forEach((el) => {
				observer.observe(el);
			});
		</script>
		<div class="z-[9999] absolute">
			<FloatingWhatsApp
				client:only="react"
				phoneNumber="5491123456789"
				accountName="Diseñá Como Yo"
				statusMessage="Normalmente responde en 1 hora"
				chatMessage="¡Hola! 👋 ¿En qué podemos ayudarte hoy?"
				darkMode={false}
				allowClickAway={true}
				allowEsc={true}
				notification={true}
				notificationSound={true}
			/>
		</div>
		<AnnouncementBar
			messages={[
				"Todos los métodos de pagos disponibles (ARS, USD, Crypto)",
				"Aprendé como vivir del diseño gráfico en 30 días.",
				"Inscribite en cuotas con MercadoPago",
			]}
			interval={15000}
		/>
		<GradualBlur
			client:load
			target="page"
			position="bottom"
			height="6rem"
			strength={1}
			divCount={2}
			curve="bezier"
			exponential
			opacity={1}
		/>
		<BlobCursor client:load />
	</body>
</html>
